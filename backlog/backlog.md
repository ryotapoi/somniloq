# Backlog

## v0.8.0

2026-07-08 maintenance audit（deep pass、6 観点: 構造 / 重複 / 結合 / エラー処理 / テスト / BSSN）の推奨タスク。import の parse 失敗診断の追加（機能、出力仕様に触る）を 1 件含むため v0.8.0。それ以外はすべて内部整理でユーザー可視の挙動変更なし。順序は上から。

- [x] `firstLine` ヘルパーの配置を集約先に揃える: outline.go:104 定義の `firstLine` が sessions.go:128 からも呼ばれており、「ファイル名 = 担当コマンド」の前提が崩れている。v0.7.2 のクロスコマンドヘルパー集約（sanitizeTSV 等）の取り残し。format.go へ移動する
- [x] `sessionRowSelect` と `scanSessionRow` の列順結合を構造で守る: SELECT 列式と `SessionRow` の scan 先を単一の列定義に対として置き、ListSessions / GetSession / LookupSessionsByID の全 session query 経路で同じ定義から導出する
- [x] orphan セッション判定述語の一本化: `NOT EXISTS (SELECT 1 FROM messages ...)` の orphan 定義が backfill.go の 3 箇所（selectBackfillTargets:194 は補集合 / CountOrphanSessions:224 / DELETE:266）に手書き重複。「数える条件」と「消す条件」がズレると destructive な backfill の挙動が静かに変わる。述語を定数 SQL フラグメントに切り出して 1 箇所に集約する
- [x] backfill.go のスキーマ移行コードを migrate_v04.go（または db_schema.go）へ分離: v0.4 移行（MigrateToV04IfNeeded / needsV04Migration / migrateToV04WithRestore / restoreForeignKeys、backfill.go:18-184 の約 160 行）が「データ補正」の backfill.go に同居し、起動時マイグレーションの canonical layer（db_schema.go）と置き場所が 2 ファイルに割れている。次のスキーマ変更時に既存移行の見落とし・順序破壊を招くため、DDL 移行を分離して backfill.go をデータ補正に限定する。分離すると既存テストファイル（db_migration_test.go / backfill_test.go）とソースの対応も 1:1 に揃う
- [x] migration SQL の `'claude_code'` リテラルを定数経由に変更する: migrate_v04.go:95,118,139 の `SELECT 'claude_code', ...` が `ingest.SourceClaudeCode`（ingest.go:7）と切り離された生リテラルで、定数を変えても migration が追随しない。ズレると複合主キー `(source, session_id)` が分裂して既存セッションが重複表示される。`?` プレースホルダに `string(ingest.SourceClaudeCode)` を渡す形へ変更する
- [x] migration の安全網テストを 2 本張る: (1) fresh v0.4 DB（db_schema.go の schema 定数経由）と v0.3→v0.4 migration 経由の DB の `sqlite_master` / `PRAGMA table_info` を突き合わせ、DDL 二重定義（migrate_v04.go:78-135 の *_new テーブルが schema 定数の複製）の片側変更を CI で落とす (2) 移行途中の任意ステップ失敗 → rollback 後に元 v0.3 テーブルが無傷であることを固定する（現状は restoreForeignKeys 失敗の注入のみで、破壊的 table-rebuild の途中失敗経路が未検証）
- [x] `SearchMessages` のフィルタ組み立てを `ListSessions` 系と統一: db_query.go:323-334 が Since/Until/Projects 条件を手組みしており、ListSessions が使う `timeFilterConditions` + `projectsCondition` の合成と二重管理。フィルタ条件を 1 つ足すたび 2 箇所を手で同期する必要があり片方だけ直すバグの温床。時刻カラム（started_at vs message timestamp、ADR 0013 の意図的差異）を引数に取る条件ビルダーへ統一して 1 経路にする
- [x] `HandleLine` のエラー時 outcome を契約に揃える: FileHandler の doc（process.go:47-50）は「non-nil error のとき outcome は無意味、runner は破棄する」と宣言しているのに、両 adapter は PersistMessage 失敗時に `LineWroteBody, err` を返している（claudecode/adapter.go:124-126 / codex/adapter.go:159-161）。今は runner が捨てるので無害だが、runner が outcome を記録する方向に変わると失敗行が誤カウントされる latent bug。`LineIgnored, err` に統一し、「error 同伴時に tx が rollback され offset が進まない」契約を ProcessJSONL のテストで固定する（エラー処理・テストの 2 観点が独立に検出）
- [x] import の parse 失敗に診断出力を付ける（機能追加）: parse/normalize 失敗行は `LineUnparsed` として件数のみ計上され、エラー内容がその場で完全破棄される（claudecode/adapter.go:108-123 / codex/adapter.go:133-158、ImportResult.Errors には載らない）。ログスキーマ変更で unparsed が急増した時に原因へ一往復で辿れるよう、代表数件（例: 先頭 5 件）だけ `ファイル:行番号: エラー内容` を ImportResult 経由で import 結果に出力する（2026-07-08 相談済み: 出力仕様の追加を許容、全件出力は不要）
- [ ] codex adapter の内部整理（3 点、1 commit）: (1) `Begin` と `HandleLine` が session_meta 解釈シーケンス（ParseRecord → type 判定 → ParseSessionMeta → h.meta 更新）を重複実装しており（adapter.go:104-121 / :137-145）、片方だけの変更で resume 時だけ meta が古くなるオフセット依存バグになるため `applySessionMeta` のような 1 メソッドに集約 (2) `codex.SessionMeta`（jsonl.go:39-46、Timestamp を持つ中間表現）が `ingest.SessionMeta`（永続化用）と同名で意味が違い読者を誤認させるため、復元用一時保持型とわかる役割名（例: sessionMetaCursor）へリネームし ingest.SessionMeta とのフィールド対応をコメントで固定 (3) `IsMessageRecord`（jsonl.go:112-113）と `NormalizeMessage`（:72-73）が同一 payload を 2 回 Unmarshal している無駄を解消
- [ ] `Store` / `BeginImport` の 1 段ラップを回収する: ingest.go:71-74 の `Store` interface は `BeginImport` 1 メソッドのみ・実装は core.DB の 1 つだけ・テストダブルとしての利用もなし。ProcessFile / ProcessJSONL が ImportTransaction 生成関数（または ImportTransaction 自体）を直接受ける形にして、投機的な named interface を除去する（BSSN: 将来 source 追加で必要になったらその時に再導入する。2026-07-08 相談済み）
- [ ] ingest 境界仕様の単体テストを ingest パッケージ内に置く: 差分インポート中核仕様の索引が core の統合テストにしかない。(1) `ProcessJSONL` の「body 未書き込みなら offset を進めない」（process.go:111-113） (2) 失敗側 — Begin がエラーを返す FileHandler / 途中で読めなくなる io.Reader を注入し、offset 据え置き・非コミットを検証（process.go:65-67, 107-109、process_test.go 自体が存在しない） (3) codex の壊れた session_meta payload（型不一致等）が LineUnparsed として計上され import が破綻しないこと（codex/adapter.go:169-175 の silent "" フォールバックの明文化） (4) claudecode `Flush` の SessionMetaWriter 型アサート失敗分岐（adapter.go:140-143、実 import 経路では到達不能なため未検証）
- [ ] cross-source の session_id 衝突テストを追加する: claude_code と codex に同一 session_id が存在した時の `resolveSessionByID` の (a) ambiguous 分岐（format.go:85-88、writeAmbiguousSessionError + exit 1）(b) not-found 分岐（:82-83）が未テスト。マルチソース設計の中核不変条件なのに、退行（片方を黙って選ぶ・panic）がテストをすり抜ける。同一 session_id を 2 ソースに投入する cmd 層テストを追加する
- [ ] 時刻フォーマットの暗黙結合を固定する: 保存側 `time.RFC3339`（import.go:176-179、秒精度）とフィルタ生成側 `"2006-01-02T15:04:05.000Z"`（filter.go:47、ミリ秒 3 桁）が別リテラルで、`started_at >= ?` の文字列比較（db_query.go:99-108)で整合することを暗黙に前提している。保存フォーマットを変えると `--since/--until` の境界セッション取りこぼしが黙って起きる。両リテラルを相互参照コメントで結び、境界一致ケース（保存値 == フィルタ境界値）の回帰テストを張る
- [ ] TSV/JSON 出力列対応の不変条件テストを張る: ADR 0012 は「JSON は TSV/Markdown の機械可読な対応物（同じスキーマ）」と定義するが、sessions の TSV（sessions.go:96-98 の 9 列）と JSON（jsonout.go:27-39 + :69-83）が列集合を独立に列挙しており、片方だけ列を足すと不変条件が黙って崩れる（outline / projects / show にも同型の対あり）。列の完全共通化は過剰抽象を招くため見送り、TSV 列と JSON フィールドの対応を突き合わせるテストで守る
- [ ] `error:` prefix の出力を集約する: エラー書式の正典は main.go:103 だが、usage 系エラーで `error: ...` を errOut に手書きする複製が 5 箇所（show.go:96 / search.go:54 / outline.go:51 / projects.go:46 / format.go:93）。prefix 変更時に片方だけ直る不整合の温床なので、共有ヘルパーに集約する（「usage エラーは直接 print、semantic エラーは main が print」の 2 系統設計自体は維持）
- [ ] 暗黙の意図・前提をコメントで明文化する（5 点、1 commit）: (1) codex/adapter.go:110-117 の Begin resume 経路の parse 失敗 silent drop は「初回 import で計上済みのため再カウントしない」意図的挙動（ADR 0009 整合）である旨 (2) import_state の `jsonl_path` はグローバル一意前提で source はキーに使わない旨 (3) claudecode / codex の `ExtractText` は許容 block type 集合が source 仕様固有（text のみ vs input_text/output_text/text）であり、同名でも揃えてはいけない旨（ADR 0005）。`ContentBlock` の同名別定義も同様 (4) `upsertSession`（db_write.go:58-73）の列集合が custom_title / agent_name を含まないのは別経路（updateSessionTitle 等）が担う意図的な使い分けである旨 (5) スキーマ変更手段の使い分け指針 — 単一列の add/drop は db_schema.go の ensure 系、テーブル構造の変更は migrate 系（table-rebuild）という 2 系統の境界（db_schema.go:12-55 / migrate_v04.go:57-170）
- [ ] architecture.md の core → adapter 依存の記述を明確化する: core/import.go:8-10 の adapter constructor 依存（importSourceSpecs への登録)は `cmd → core → ingest/...` の正規ルート依存であり、ADR 0008 例外（SessionMetaWriter のコンパイル時確認限定）とは別物。現行の「例外として…source 固有 adapter 全般への自由な依存は認めない」の文言が constructor 依存まで例外扱いと誤読させる（本監査でも誤読を誘った）ため、両者を区別して記載する
