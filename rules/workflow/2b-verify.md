# Step 2b: 動作確認

自動テストではカバーできない挙動（CLI 出力のフォーマット、対話的な挙動、実 DB・実ログに対する副作用など）がある場合、コマンドを実行して出力が期待通りか確認する。必要ならユーザーにも出力を貼ってもらって確認を取る。

自動テストで十分にカバーできている変更のみの場合はスキップしてよい。

ユーザー確認が必要と判断したのに通過していない状態で、次（レビュー）に進んではならない。

## 動作確認が完了したら

次のサブステップに進む: rules/workflow/2c-review.md を読む。
