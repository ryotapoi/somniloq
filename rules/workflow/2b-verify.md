# Step 2b: 動作確認

実装が完了したら、**`/verify-user-check` スキルを必ず呼ぶ**。このスキルが差分を検査して「ユーザー動作確認が必要か」を判定する。

- 要確認と判定された場合: CLI 出力・対話的な挙動・実 DB/ログへの副作用などを実際に動かして確認する。必要ならユーザーにコマンド実行と出力共有を依頼する。通過してから次へ進む
- 不要と判定された場合: そのまま次へ進む

次（レビュー）に進むのはユーザー確認を通過してから。
